<%= form_for(@category) do |f| %>
 		<%= f.label :category_name %>
		<%= f.text_field :category_name %><br><br>
        <%= f.submit "Save changes", :class => 'altbtn altbtn-lg altbtn-outline' %>
<% end %>